apiVersion: v1
kind: Service
metadata:
  name: {{ include "helep.fullname" . }}
  labels:
    {{- include "helep.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "helep.selectorLabels" . | nindent 4 }}
