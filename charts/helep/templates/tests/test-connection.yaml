apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "helep.fullname" . }}-test-connection"
  labels:
    {{- include "helep.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "helep.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
