apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "dispatch-service.fullname" . }}-test-connection"
  labels:
    {{- include "dispatch-service.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "dispatch-service.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
