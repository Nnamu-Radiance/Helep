apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "sos-service.fullname" . }}-test-connection"
  labels:
    {{- include "sos-service.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "sos-service.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
