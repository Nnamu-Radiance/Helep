apiVersion: v1
kind: Service
metadata:
  name: {{ include "sos-service.fullname" . }}
  labels:
    {{- include "sos-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "sos-service.selectorLabels" . | nindent 4 }}
